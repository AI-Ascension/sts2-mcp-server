# Consumed `runtime-v1` artifact

This checked-in copy binds the live MCP mapping to the protocol owner's release-like
`sts2-protocol/runtime-v1` schema. It is inert contract data: the MCP server still owns framing,
tool validation, and fixed gateway mapping, while the game-mod remains authoritative for host
state and effects.
